// because otherwise we will search beyond the range of the iterator
// the "end" must be substracted from "begin" so that we don't go
// "begin" amount of items beyond the range of the iterator
int main(void) {}
