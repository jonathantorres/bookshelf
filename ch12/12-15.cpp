// It doesn't need to since it's the job of the code that is using the stream
// our implementation of >> will simply return it back after not fulfilling
// the condition on the first while loop and the if() statement
int main(void) {}
